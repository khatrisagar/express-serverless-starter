{
  "name": "user-api",
  "version": "1.0.0",
  "main": "app.ts",
  "scripts": {
    "dev": "nodemon --exec ts-node ./src/server.ts",
    "build": "tsc && tsc-alias -p tsconfig.json",
    "lambda:build": "call scripts/postbuild.bat && npm run build",
    "sam:build": "sam build -t ./sam-template.yml",
    "sam:local": "sam local start-api"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "@codegenie/serverless-express": "^4.15.0",
    "express": "^4.21.0",
    "source-map-support": "^0.5.21"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/node": "^22.5.5",
    "cp": "^0.2.0",
    "ts-node": "^10.9.2",
    "tsc-alias": "^1.8.10"
  }
}
